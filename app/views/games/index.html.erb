<h2>Games</h2>

<ol>
  <% @games.each do |game| %>
    <li>
      <%= link_to game.title, game %> for the <%= game.consoles.map { |console| link_to console.name, console }.to_sentence.html_safe %> published by <%= link_to game.publisher.name, game.publisher %>
    </li>
  <% end %>
</ol>
